/target
**/*.rs.bk

# Local environment. Never commit real credentials.
.env
.env.local

# sqlx offline query metadata.
# Deliberately NOT committed: the approved conditions require the schema and
# query validation to be reviewed before generated metadata enters the repo.
# M0 uses only runtime-checked queries, so nothing here is generated yet.
/.sqlx
